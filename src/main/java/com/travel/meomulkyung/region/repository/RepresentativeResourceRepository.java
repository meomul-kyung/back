package com.travel.meomulkyung.region.repository;

import com.travel.meomulkyung.region.domain.RepresentativeResource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepresentativeResourceRepository extends JpaRepository<RepresentativeResource, Long> {
}
